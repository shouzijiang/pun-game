---
alwaysApply: true
---
# UniApp 多端 API 规范

本工程需同时兼容 **微信小程序**、**抖音小程序** 和 **H5**。禁止使用仅浏览器或仅某一端可用的 API，一律使用 **uni.*** 提供的封装。

## 网络请求

- **用** `uni.request`，**不用** `fetch`、`axios`、`XMLHttpRequest`。
- 示例：`uni.request({ url, method: 'GET', success, fail })`。

## 存储

- **用** `uni.getStorageSync` / `uni.setStorageSync`（或异步 `uni.getStorage` / `uni.setStorage`），**不用** `localStorage`。

## 路由与页面

- **用** `uni.navigateTo`、`uni.navigateBack`、`uni.reLaunch`、`uni.redirectTo`，**不用** `window.location`、`history.pushState`。

## 交互与系统

- 提示：`uni.showToast`、`uni.showModal`。
- 剪贴板：`uni.setClipboardData` / `uni.getClipboardData`。
- 其他能力查阅 [UniApp  API 文档](https://uniapp.dcloud.net.cn/api/)，优先使用 `uni.xxx`。

## 注意

- 小程序环境下没有 `window`、`document`、`fetch`，使用 uni 封装可保证三端一致行为。
